print()

"""
# range() 函数是一个内置函数，用于生成一个整数范围序列。它的常见用法是在循环中迭代一定次数。
# range() 函数的基本语法如下：
# range(start, stop, step)
参数说明：

start（可选）：表示范围的起始值，默认为0。
stop：范围的结束值（不包含在范围内）。
step（可选）：每次迭代之间的步长，默认为1。
"""
for i in range(1, 10, 2):
    print(i)

"""
# 输出
1
3
5
7
9
"""
